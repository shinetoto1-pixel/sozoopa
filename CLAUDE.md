# 프로젝트 컨벤션

이 저장소에서 작업할 때 적용되는 사용자 단축 명령어/컨벤션 목록.
노트북과 이 컴퓨터 등 여러 환경에서 동일하게 적용되도록 여기에 기록한다.

## 세션 시작 시 필수 확인

새 세션을 시작하면 **가장 먼저 `git pull`부터 실행**한다. 다른 환경(회사/집 등)에서 push한 최신 변경사항을
받아오지 않으면 로컬에 남은 예전 파일을 보게 되므로, 변경사항이 없을 걸로 예상되더라도 매번 실행한다
(변경 없으면 "Already up to date" 한 줄로 끝나는 가벼운 명령어라 손해 볼 게 없다).

pull 이후 **`세션로그.md`의 가장 최근 1~2개 날짜 항목만 확인**한다 (파일 전체를 읽지 않는다 — 누적되면서
파일이 길어져도 매번 읽는 양이 늘어나지 않도록). 다른 환경(회사/집 등)에서 있었던 대화 내용과 그로 인한
변경사항이 요약돼있으므로, 사용자가 다시 설명하지 않아도 최근 맥락을 파악한 상태로 작업을 이어간다.
의미있는 결정(방법론 변경, 새 컨벤션 등)이 있었던 세션이 끝나면 `세션로그.md`에 새 항목을 날짜별로 추가한다.

## 세션 마무리 신호 (정확한 문구가 아니라 "의도"로 인식)

사용자는 매번 똑같은 문구를 쓰지 않는다. "퇴근해야겠다 집에 가서 하자", "출근해야되니까 회사가서 하자",
"오늘은 여기까지 하고 집에서 하자", "오늘은 그만하고 내일하자", "나머지는 집에서/회사가서 이어서 하자" 같은
**다른 환경에서 이어가겠다는 취지의 말이면 표현이 달라도 전부 같은 신호**로 인식한다.

이 신호가 감지되면 사용자가 "깃 그거해줘"라고 따로 말하지 않아도 자동으로:
1. 오늘 세션에서 있었던 논의·결정사항을 `세션로그.md`에 새 날짜 항목으로 요약해서 추가
2. 변경된 파일 전부 커밋 + 푸시 (커밋 메시지는 알아서 작성)

여기까지 하고 나서 평소 쓰던 톤 그대로(존댓말, 반말 아님) "정리해서 올려뒀습니다" 정도로 짧게 확인해준다.
(이 문구 자체를 그대로 따라 하지 말 것 — 예전에 여기 반말 예시가 적혀 있어서 그걸 그대로 복사했다가
어투가 갑자기 바뀌는 문제가 있었음. 톤은 항상 평소 대화 톤을 유지하고, 길이만 짧게 줄인다.)

## 단축 명령어

- **"깃 그거해줘"** = 지금까지의 변경사항을 git에 커밋하고 원격 저장소에 푸시해줘
  (커밋 메시지는 변경 내용을 보고 알아서 작성, 별도 확인 없이 진행)
